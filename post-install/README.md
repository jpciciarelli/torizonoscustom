## Archivos necesarios post-instalación

El archivo docker-compose.yml ejecuta los contenedores automáticamente y pone en UP el can0. De esta forma la aplicación está lista para usar cuando enciende el módulo.

El archivo debe copiarse al directorio:

`/var/sota/storage/docker-compose/docker-compose.yml`
